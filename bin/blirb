#!/usr/bin/env ruby

require_relative '../lib/blirb'

# # if a tasks file is passed in, parse the yml-based tasks and feed them to blirb

if ARGV[0]
  blirb = BlirbCoordinator.new ARGV.shift
else
  blirb = BlirbCoordinator.new
end

blirb.go